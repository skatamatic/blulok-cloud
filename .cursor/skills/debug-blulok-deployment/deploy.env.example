# Copy to deploy.env (gitignored). Used by all debug-blulok-deployment scripts.

BLULOK_DEBUG_ADMIN_PASSWORD=DevAdmin123!@#
# Cloud SQL via proxy on localhost (start proxy separately — see reference.md)
BLULOK_DEBUG_DB_PASSWORD=

# Optional: BLULOK_DEBUG_ENV=develop
