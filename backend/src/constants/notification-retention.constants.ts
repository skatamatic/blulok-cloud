/** Per-user retention for read in-app notifications (rolling window). */
export const NOTIFICATION_READ_RETENTION_DAYS = 30;

/** Per-user retention for unread in-app notifications (rolling window). */
export const NOTIFICATION_UNREAD_RETENTION_DAYS = 90;
