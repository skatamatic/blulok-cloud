/** Max rows retained per gateway after each ingest trim. */
export const GATEWAY_TELEMETRY_LOG_RETENTION = 10_000;

/** Max log lines accepted in a single ingest request. */
export const GATEWAY_TELEMETRY_LOG_MAX_INGEST_BATCH = 500;
