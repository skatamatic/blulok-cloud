# Prefer 127.0.0.1 over localhost on Windows — localhost often resolves to ::1,
# and another process can own IPv6 :3000 while BluLok listens on IPv4.
VITE_API_URL=http://127.0.0.1:3000
VITE_WS_URL=ws://127.0.0.1:3000
