# Dependencies and build output (rebuilt inside the image)
node_modules
dist
coverage
.nyc_output

# Local env / logs
.env
.env.*
*.log
npm-debug.log*

# Editor / OS noise
.vscode
.idea
.DS_Store
Thumbs.db

# NOTE: public/bludesign/environment (HDR/EXR/textures) must NOT be excluded —
# Vite copies public/ into dist/ at build time for the Facility Viewer.
