{
  "name": "blulok-cloud",
  "version": "1.0.0",
  "description": "Blulok Cloud - Secure Storage Management System",
  "scripts": {
    "test:backend": "cd backend && npm test",
    "test:frontend": "cd frontend && npm run test:all",
    "test:all": "npm run test:backend && npm run test:frontend",
    "install:all": "npm install && cd backend && npm install && cd ../frontend && npm install",
    "dev:mysql:up": "docker compose -f docker-compose.mysql.yml up -d",
    "dev:mysql:down": "docker compose -f docker-compose.mysql.yml down",
    "dev:mysql:logs": "docker compose -f docker-compose.mysql.yml logs -f mysql"
  }
}